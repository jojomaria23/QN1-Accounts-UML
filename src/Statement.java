/**
 * QUESTION ONE - Bank Accounts UML
 * «interface» Statement
 * Declares the single behaviour every account must expose: the
 * ability to produce a printable statement of itself.
 */
public interface Statement {
    String generateStatement();
}
